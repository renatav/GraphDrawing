package graph.elements;

/**
 * Directions of edges. Used if the graph is undirected,
 * but the direction is still important in the context of
 * an algorithm
 * @author Renata
 */
public enum EdgeDirection {

	TO_DESTINATION, TO_ORIGIN;
}
